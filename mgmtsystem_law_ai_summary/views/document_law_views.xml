<odoo>
  <data>

    <record id="view_law_form" model="ir.ui.view">
      <field name="name">document.law.form</field>
      <field name="model">document.law</field>
      <field name="inherit_id" ref="document_law.view_law_form"/>
      <field name="arch" type="xml">

        <field name="rss_html" position="replace">
          <field name="rss_html" widget="html" placeholder="e.g. Once upon a time..." required="0" options="{'safe': True, 'codeview': True, 'collaborative': True}" invisible="law_summary"/>
          <field name="law_summary" invisible="not law_summary"/>
        </field>

        <page name="info" position="after">
          <page name="main document" string="Main Document" invisible="not law_summary">
            <group>
              <group>
                <field name="rss_html"/>
              </group>
            </group>
          </page>
        </page>

      </field>
    </record>

  </data>
</odoo>
